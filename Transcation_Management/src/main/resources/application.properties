spring.application.name=Transcation_Management

# Database
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}

# Server
server.servlet.context-path=/api/v1.0

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Brevo API
BREVO_API_KEY=${BREVO_API_KEY}
BREVO_FROM_EMAIL=${BREVO_FROM_EMAIL}

# JWT
jwt.secret.key=s3VdG2YxUuD9xG1gQ4f5F7H8kI9LmN0pQrStUvWxYzAaBbCcDdEeFfGgHhIiJjKkL

# Frontend URL
money.manager.frontend.url=${MONEY_MANAGER_FRONTEND_URL}
app.activation.url=${MONEY_MANAGER_BACKEND_URL}

# Profile
spring.profiles.active=prod
